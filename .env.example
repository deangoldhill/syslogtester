# Use a long, unique random value. Example: openssl rand -hex 32
SYSLOG_UI_TOKEN=replace-this-with-a-long-random-secret

# Web dashboard port on the Docker host (because host networking is used).
WEB_PORT=8080
